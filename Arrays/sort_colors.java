//Salesforce
class Solution {
    public void sortColors(int[] nums) {
        Arrays.sort(nums);
    }
}
//as red=0 white=1 and blue=2 we need to arrrange all reds then white and then blue so just sort array
